@model List<DBModel.ET.Side>
    @{
        var total = Model.Count();
    }
<div class="slider-wrapper theme-default">
    <div id="slider" class="nivoSlider">

        @for (int i = 0; i < total; i++)
        {
            var name = Model[i].Description;
            if (name.Length > 0)
            {
                var c = "#htmlcaption" + @i;
                <a href="@Model[i].Link" target="@Model[i].Target"><img src="@Model[i].Image" data-thumb="@Model[i].Image" alt="" title="@name" /></a>
            }
            else
            {
                <a href="@Model[i].Link" target="@Model[i].Target"><img src="@Model[i].Image" data-thumb="@Model[i].Image" alt="" /></a>
            }

        }
    </div>
  
    </div>